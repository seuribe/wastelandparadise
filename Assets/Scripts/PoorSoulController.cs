using UnityEngine;
using System.Collections;

public class PoorSoulController : MonoBehaviour {

	// Use this for initialization
	void Start () {
	    
	}
	
	// Update is called once per frame
	void Update () {
	    // 1. if unconverted
        // 1.1. if player is around, head for him
        // 1.2. if it's not around, mode randomly
        // 2. if converted
        // 2.1. if player is around, follow him peacefully
        // 2.2. if not, stay in place enjoying your inner peace
	}
}
